# frozen_string_literal: true

RSpec.describe Numruby::Linalg do
  it 'has a version number' do
    expect(Numruby::Linalg::VERSION).not_to be_nil
  end

  it 'does something useful' do
    expect(false).to be(true)
  end
end
